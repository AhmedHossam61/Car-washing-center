# Camera Setup

Use fixed gate cameras aimed at the plate region where possible. Configure each camera in
`CAMERAS_JSON` with an `entry`, `exit`, or `both` role. Keep RTSP credentials out of git by
storing them only in `.env`.
